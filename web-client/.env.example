# ── Whispro Web Client ───────────────────────────────────────────────────
# Vite exposes only variables prefixed with VITE_ to the browser bundle.

# WebSocket relay server URL
VITE_RELAY_URL=ws://localhost:8080

# Key distribution server URL
VITE_KEY_SERVER_URL=http://localhost:8000
